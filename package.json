{
  "name": "tfa-mcp",
  "version": "0.1.0",
  "description": "Lean Agent Task Framework – MCP stdio server (TypeScript)",
  "type": "module",
  "bin": {
    "mcp-tfa": "dist/mcp/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-prompts.mjs",
    "clean": "rm -rf dist",
    "mcp": "npm run build && node dist/mcp/server.js",
    "test": "npm run build && node --test",
    "install-global": "npm run build && npm uninstall -g mcp-tfa && npm install -g ."
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@xenova/transformers": "^2.17.2",
    "semver": "^7.7.2",
    "sharp": "^0.34.3",
    "sql.js": "^1.11.0",
    "zod": "^3.23.8"
  },
  "license": "UNLICENSED"
}
